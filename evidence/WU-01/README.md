# WU-01 Evidence

WU-01実行時の再検証可能な結果を保存する。

- `build.log` — `xcodebuild build`出力
- `test.log` — `xcodebuild test`出力
- `git-verification.txt` — repository visibility、local/remote SHA、working tree状態

ログはcredential、token、個人情報を含まないことを確認してからcommitする。

